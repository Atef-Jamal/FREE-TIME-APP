{
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\"",
    "build": "concurrently \"cd frontend && npm run build\" \"cd backend && npm run build\"",
    "start": "concurrently \"cd frontend && npm run build && npm run preview\" \"cd backend && npm run build && npm run start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
